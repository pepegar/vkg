box: wercker/default

build:
    steps:
        - script:
            name: Test
            code: |-
                go get github.com/axw/gocov/gocov
                gocov test ./... > coverage.json

        - script:
            name: Coverage
            code: gocov report coverage.json
